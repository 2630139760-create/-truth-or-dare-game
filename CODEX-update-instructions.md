# 真心话大冒险：本轮完整修改说明

请修改现有项目。先完整读取本文件及同目录 game-question-bank.json。本说明及新版题库优先于旧制作说明中的冲突要求。不要重建项目。

## 一、按钮文字整体旋转

目前“开始”两个字竖着堆叠，方向错误。保留左右按钮的竖向圆角长方形外框、位置及点击区域，仅旋转内部整段文字：

- 文字先正常横排为一行“开始”或“暂停”，两个字不换行。
- 左侧整段文字顺时针旋转90°；右侧整段文字逆时针旋转90°。
- 不使用竖排 writing-mode，不用 br，不逐字旋转。
- 内部 span 可使用 display:inline-block、white-space:nowrap、writing-mode:horizontal-tb、flex-shrink:0、transform-origin:center，再分别 rotate(90deg) 与 rotate(-90deg)。
- 注意旋转前容器不能让文字换行，旋转后必须居中、不裁切。不要把整个按钮连同外框旋转。
- 开始、暂停两种文字状态均适用，左右同步。
- 验收：头正立时看到整段侧转的文字，歪头后可读到正常横排“开始／暂停”，不是两个正立字上下排列。

## 二、指针最高速度每秒5圈

- 全部号码、真心话、大冒险及强制重抽转盘，统一最高角速度1800°/秒，替换旧720°/秒。
- 从慢到快约1秒，随后匀速；按暂停后约1秒平滑减速到零。加减速曲线保持水平镜像，延续原有约1秒的变速时间规则。
- 用真实时间戳计算角度，不按帧数固定递增；避免CSS最短角度插值吞掉整圈。
- 指针旋转，底盘不转；每次从当前角度继续，不归位。同场游戏保留各转盘角度。
- 普通抽取从开始到完全停止最长10秒，减速包含在10秒内。
- 强制重抽总旋转时间随机3–5秒，包含加减速；两侧控制按钮隐藏；停转2秒后弹出全屏红字结果。
- 更新目标角度、累计圈数、加减速积分及重抽时长计算，不能只修改视觉动画常量。
- 手动暂停按实际终止角度选题。若重抽提前选目标，应计算满足角速度和时长要求的轨迹；不允许停止时瞬移针尖或暗改结果。
- 避免固定整圈速度乘固定时长导致自动停止永远落同一区域。保留实际随机性和几何一致性。

## 三、题目转盘文字靠外缘

目前文字全挤在中心轴附近。需要修改文字坐标和排版：

- 半径记为R，中心约0–35%R不放题目文字。
- 每道题沿所属扇形中心射线从内向外阅读，主要文字区间约42%–94%R，末端靠近外缘并留少量边距。
- 保留完整正文，不只放序号、关键词或省略号；结果弹窗仍用大字号显示完整正文。
- 根据实际题数、扇形宽度、题长和半径动态适配字号、字距、可用径向长度。
- 使用SVG或Canvas等可精确控制的绘制方式。逐扇形裁剪作防护，但不能依靠裁剪隐藏题尾。
- 文字不越界、不覆盖邻格，不要厚重阴影。检查旋转中心与text-anchor，避免再次把全部文字锚在轴心。
- 指针位于文字上层，题目轮盘针尖足够细，不能同时盖住多个窄扇形。
- 号码转盘的大数字继续保持清晰、靠外的位置。
- 真心话、大冒险复用排版逻辑。兼容iPad横屏全屏、Safari分屏及窗口大小变化。
- 133格每格约2.7068°，102格约3.5294°。窄扇形长题可以使用小字，但不能声称小屏上与弹窗同样清晰；重点验证文字已外移、无重叠、无截断。

## 四、更名与完整题库替换

| 原名称 | 新名称 |
| --- | --- |
| 纯情档 | 休闲档 |
| 暧昧档 | 暧昧档 |
| 猎奇档 | 刺激档 |

统一更新选档页、游戏标题及帮助说明。内部枚举可保持原值，避免无关重构。本JSON使用innocent、flirty、extreme，导入时适配项目现有类型。

唯一可用题目来源是 game-question-bank.json 的 groups[].questions；题目正文为 text。

| 档位 | 真心话 | 大冒险 |
| --- | ---: | ---: |
| 休闲档 | 100 | 100 |
| 暧昧档 | 100 | 100 |
| 刺激档 | 133 | 102 |

合计635道。用户明确要求：最初自动生成的旧题库全部移除，不保留为可被应用加载的备用题库。

- 删除当前源码中的旧题数组、旧题生成逻辑、默认回退题和相关运行资源引用；不能仅追加新题或改名。
- 不改写Git历史，不误删游戏逻辑和用户的新题。
- 新题按正文、原条件、时长、类别导入，不自行扩写、补题凑数。来源图片的问答式大冒险仍按用户分类保留，不自行换类。
- 题库独立于界面与转盘逻辑，六组分别维护。若拆成六个文件，逐项正文必须与本JSON一致。
- 刺激档真心话从139条图片记录去重6条后保留133道；其中3条跨图重叠、3条同义重复。
- 刺激档大冒险原图109道，合并近义重复2道、另排除风险题5道，保留102道。排除项来源：IMG_2832.jpeg第5、6、9、11题，IMG_2836.jpeg第61题；原因见excludedQuestions。不要重新导入。
- deduplication、excludedQuestions、reviewNotes和notes是审计资料，不是题目；其中removed.text也不能再次加入抽取列表。
- 跨档相似题保留；休闲、暧昧四组按用户指定各100道保留。刺激档已经完成保守去重，不再擅删条件不同的相似题。
- 原图少量措辞疑点见reviewNotes，暂按原文保留。

## 五、按实际题数划分转盘，并迁移旧缓存

- N必须来自当前组questions.length；扇形角度为360/N。渲染、随机抽取、结果索引、边界判定使用同一N。
- 排查写死100、99、3.6的逻辑。普通抽取覆盖全部N道；重抽排除原题ID后在实际N-1道中选择。
- 使用稳定题目ID，不用旧题库数组索引恢复题目。弹窗应直接引用针尖所在扇形对应的新题对象。
- 检查sessionStorage、localStorage及现有Service Worker是否仍缓存旧题或旧索引。
- 使用JSON中的contentVersion：user-bank-2026-09-17-v2。旧会话与新版题库不兼容时，明确提示题库已更新并使旧会话失效，重新开局，避免继续抽到旧题。
- 迁移仅在版本不兼容时触发。新版同一场游戏每轮仍保留指针角度；确认退出再开始新场才归位。
- 对空题库或只有1题的情况作防御处理，避免除零或无限重抽；当前六组正式数据均非空。

## 六、保留已有流程

保留首页、选档、人数输入、抽号码、停留2秒、真心话／大冒险选择、抽题、完成、一次重抽、退出确认和全屏功能。

保留按钮颜色、左右状态同步、遮罩、弹窗布局、重抽后的全屏红字结果。完成后仍回到号码转盘。

互动须经对方同意，可拒绝或停止；一次重抽限制不代表强制实施动作。涉及相册、通讯录、发消息等题目仅显示游戏文字，不实现自动读取、发送、删除或发布。

## 七、验证及交付

1. 在最新main上建立本轮新分支；旧PR已合并，不再更新旧PR。若已存在专用于本轮修改且未合并的分支，检查后继续该分支，避免重复PR。
2. 确认六组数量依次100、100、100、100、133、102，总计635；ID唯一；逐题正文与JSON一致，无旧题混入，无审计内容被误当作题目。
3. 运行现有测试和生产构建。增加必要验证，覆盖N=133、N=102的角度边界、首尾扇形与重抽排除原题。
4. 验证匀速区间任意连续1秒累计1800°；普通抽取在10秒内完全停止；重抽旋转总时长3–5秒；停止结果与针尖一致；同场角度正确保留。
5. 尽可能提供真实浏览器的iPad横屏截图：玩家轮盘、133格真心话轮盘、102格大冒险轮盘；验证按钮整体旋转和外圈排版。若无法运行浏览器，明确说明未验证项，不能把构建通过等同于视觉验收。
6. 保留GitHub Pages工作流和生产base：/-truth-or-dare-game/。提交修改并创建或更新本轮PR，说明测试、构建和未完成项，不自动合并。

完成后给出：旧题入口及缓存处理、六组实际数量、指针速度、按钮旋转方向、文字径向位置，以及真实浏览器验证结果。
