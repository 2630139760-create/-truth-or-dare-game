# 真心话大冒险网页项目：可直接交给 Codex 的制作说明

请根据以下说明制作一款可实际游玩的中文“真心话大冒险”网页应用。优先适配平板横屏，兼容电脑、手机及触屏。需要完整交互、真实转盘动画和完整题库，不能只制作静态样稿。

以下是明确后的实现规格。猎奇档保留用户原始描述，不改为“非露骨版本”；下表仅整理用户已经提供的内容范围，不额外扩写具体题目。各档位和转盘、弹窗、重抽流程按下述要求实现。

**1．产品结构与题库**

整个流程为：
首页 → 选择档位 → 输入人数 → 抽取玩家号码 → 玩家选择真心话或大冒险 → 抽取题目 → 完成，或使用一次强制重抽 → 回到号码转盘。

| 档位 | 真心话内容 | 大冒险内容 |
| --- | --- | --- |
| 纯情档 | 轻松、日常、兴趣、趣事等绿色话题 | 日常、轻松、有趣的小游戏与挑战 |
| 暧昧档 | 恋爱经历、交往次数、恋爱观、人生观、世界观及适度私密话题 | 点到为止的暧昧互动、对视，以及双方同意的轻度接触 |
| 猎奇档 | 保留用户原始描述：“真心话会包含很多没下限的问题” | 保留用户原始描述：“大冒险可能就是做爱舌吻或者说脱衣各种各样的东西” |

- 每个档位分别准备100条真心话、100条大冒险，共6组、600条。
- 每组100条内容互不相同，不能使用重复句式换几个词凑数，也不能留下占位文本。
- 同一档位的两类题目分开储存；切换档位只改变题库和档位文字，不改变页面布局、按钮位置或游戏规则。
- 题库与界面、转盘逻辑分离。每条题目有稳定且唯一的ID、所属档位、类别和完整正文，方便后续修改。
- 涉及他人的互动以双方愿意为前提。重抽后的“没有选择权”只表示没有第二次重抽机会，不表示必须接受身体接触。

**2．首页、选档与人数输入**

首页：
- 中央大标题“真心话大冒险”。
- 使用爱心、眨眼、暧昧短标签、贴纸等装饰，营造轻松的聚会气氛。
- 标题下方为显眼的“开始”按钮。
- 视觉精致、活泼，装饰不遮挡功能区。

点击首页“开始”：
- 创建一场新游戏，进入档位选择。
- 显示“纯情档”“暧昧档”“猎奇档”三个选项。
- 选择档位后进入人数输入页。
- 数字输入框配“发送”按钮，同时支持键盘回车提交。
- 第一版人数范围暂定2—20人，默认4人，范围集中配置。拒绝空值、非整数和越界值，并就地显示简短提示。
- 提交有效人数后生成本场的转盘配置，进入号码转盘。

全屏：
- 提供不抢眼的进入／退出全屏图标。
- 通过用户点击请求浏览器全屏；不支持时使用铺满可用视口的布局。
- 退出浏览器全屏、旋转设备或调整窗口大小，都不结束本场游戏，也不重置转盘。

**3．统一视觉与退出规则**

从点击首页“开始”后的所有页面起，左上角始终显示红色“×”，包括所有弹窗以及重抽后的全屏结果页。

点击“×”：
- 弹出退出确认框，提示文字为“是否退出”。
- 确认框位于屏幕中央，外框为直角长方形，不能做圆角卡片。
- 横屏平板上，宽、高分别约为视口的1/3，面积约为全屏1/9。
- 上部居中显示“是否退出”，文字本身没有边框。
- 下部水平排列“确定”和“取消”，两按钮等宽，两者之间留出约一个按钮宽度的空隙。
- 两按钮均为圆角长方形，带可见边框。
- 点击“确定”才结束当前场，清空本场状态并回到首页。
- 点击“取消”关闭退出框，准确恢复原页面、原弹窗和原进度。

统一按钮规则：
- 带文字的普通按钮使用圆角长方形和可见边框。
- 唯一明确例外：选择“真心话／大冒险”的两个按钮为大圆形。
- 左上角“×”及全屏按钮属于图标按钮，不必套用文字按钮造型。

统一弹窗规则：
- 弹窗外框全部为直角长方形。
- 非全屏弹窗出现时，在弹窗背后加半透明黑色遮罩，建议透明度0.32；仍然能看清后面的轮盘和按钮。
- 遮罩阻止点击穿透。点击遮罩不关闭弹窗。
- 退出框优先级最高；退出确认期间，原来的结果弹窗保留在其下方。
- 弹窗面积比例以横屏平板为基准，小屏幕以文字完整、按钮可点击为优先，允许适当扩大。
- “×”始终可访问，但不能连续打开多个退出确认框。

**4．号码转盘的布局、颜色与编号**

- 屏幕中央显示大圆盘，圆盘本身不转动，只有指针顺时针旋转。
- 根据游玩人数N，将圆盘平均分为N个扇形，每个扇形角度严格为360°/N。
- 每场新游戏随机生成并分配有区分度的颜色。随机应来自受约束的配色，不应完全无约束地随机RGB，避免全部颜色过近、过暗或过亮。
- 各扇形显示清晰的大号号码。号码和文字对比度必须足够，不能只靠颜色识别玩家。
- 本场游戏中，号码、颜色和位置的对应关系固定；每轮抽取、切换页面和重抽都不能重新洗牌。
- 1号扇形的中心线朝正上方，即12点钟方向。顺时针依次为2号、3号……N号。
- 指针轴心固定在圆盘中心，新游戏的初始指针指向正上方，落在1号扇形内部，不能正好落在1号与其他号码的分界线上。
- 每个轮盘保留自己的角度。再次显示该轮盘时，不播放归零动画，也不偷偷修改角度。

圆盘左右各放一个同步控制按钮：
- 待开始时：绿色“开始”。
- 转动时：红色“暂停”。
- 左按钮文字在屏幕坐标下顺时针旋转90°，便于坐在平板左边的人阅读。
- 右按钮文字逆时针旋转90°，便于坐在平板右边的人阅读。
- 两侧按钮控制同一个转动状态，不能创建两套动画。
- 点击区域足够大，不依赖鼠标悬停。

**5．指针运动与停止规则**

这里的“暂停”表示“减速至停止并结算本次抽取”，不是暂时冻结后继续同一次抽取。

普通抽取的规则：
1. 点击任意一侧“开始”，从该轮盘当前保留的角度开始顺时针旋转。
2. 两侧按钮立即同步为红色“暂停”。
3. 指针由静止平滑加速，最高角速度为720°/秒，也就是每秒两圈。
4. 到达最高速度后保持匀速，直到玩家点击“暂停”或触发自动停止。
5. 点击“暂停”后，从当前实际角速度平滑减速至0，不瞬停、不反转、不突然跳速。
6. 指针完全停稳后，才依据最终角度确定结果。
7. 本次启动到完全停稳，最长为10秒。无人点击暂停时，要在第10秒停稳，不能等第10秒才开始减速。
8. 减速期间禁止重复暂停；停稳后的等待及弹窗期间，禁止再次启动本轮转盘。

为避免固定720°/秒、固定1秒加减速、固定10秒总时长导致每次自动停止恰好转完整圈，本版采用以下明确优化：
- 每次转动开始时，随机取d∈[0.75,1.25]秒，作为本次正常加速和减速的共同持续时间。
- 同一次转动的加速、减速使用同一个d，约1秒，并且速度曲线在时间方向上互为镜像。
- 可使用S(u)=3u²−2u³，u∈[0,1]：
  - 加速角速度：720×S(u)。
  - 从满速减速：720×S(1−u)。
- 没有手动暂停时，从第10−d秒开始减速，在第10秒停稳。
- 如果玩家在加速途中就按暂停，从当时的实际速度开始减速，使用相同缓动形状缩放至该速度，不能先跳到满速。
- 随机d只在启动时确定一次，不能每帧重抽。

实现时使用基于实际经过时间的角度计算，例如requestAnimationFrame配合单调时钟。不能按固定“每帧增加几度”实现，以免设备帧率改变转速。加速、匀速和减速的交界必须连续。

**6．抽中玩家与选择类别**

- 号码转盘完全停稳后，先保持当前画面2秒。
- 等待期间禁用转盘按钮。
- 随后弹出类别选择框：直角长方形，居中，横屏平板宽、高分别约为视口1/2，面积约为全屏1/4。
- 顶部居中显示：“X号 请你选择：”
- 其中“X号”使用该玩家扇形的颜色；“请你选择：”使用正常黑色。玩家配色同时要适合在浅色弹窗中显示。
- 下方并排放两个较大的圆形按钮：“真心话”和“大冒险”。
- “真心话”文字为粉色，“大冒险”文字为青蓝色，背景及边框保证可读性。
- 选择后关闭此弹窗，进入当前档位、当前类别对应的题目转盘。
- 保存本轮玩家号码、颜色及类别。抽题途中不能换类别，也不能跳回号码页重选玩家。

**7．100格题目转盘**

- 整体布局与号码转盘保持一致：大圆盘居中、左右开始／暂停按钮、左上角“×”。
- 指针运动与按钮逻辑使用同一个转盘引擎，不另写行为不同的动画。
- 唯一的转盘内容差异：平均分成100个扇形，每格严格3.6°，对应所选题库中的一条题目。
- 第一格中心线仍朝正上方；题目顺序、题目ID与扇形编号在本场内固定。
- 每格都必须绘制对应的完整题目文字，按沿半径从中心方向读向外缘的顺序排版。可以留出中心轴心周围的空白区域，不要求从圆心本身开始写字。
- 根据实际可用长度与扇形宽度计算字号；允许很小，但不能越界、压进相邻扇形。
- 使用清晰的矢量绘制或高分辨率渲染，并给每个扇形设置裁剪区域，防止文字溢出。
- 不得未经说明就只显示“题目1”或用省略号取代完整题目。
- 100条长文字在平板上不可能同时保证舒适阅读；盘面完整文字用于展示，抽中后的大弹窗负责清晰阅读全文。
- 程序必须通过角度、索引和题目ID判定结果，不使用OCR，不依赖AI看清盘面小字。
- 100格不要求100种人眼明显可区分的颜色，可以使用协调的多色配色，让相邻扇形有区分，避免极低对比度。
- 指针使用细线和尖端，尖端的中心线是唯一判定依据。中心装饰不要遮挡太多内容。

首次抽题停稳后，直接弹出结果框；这里不额外等待2秒。号码抽取与强制重抽后的等待时间仍严格为2秒。

**8．首次题目结果与一次重抽**

首次结果框：
- 居中，直角长方形，面积约为全屏1/2。
- 横屏平板可将宽、高分别设为约70.7%视口，以接近指定面积。
- 上部居中显示抽中的完整内容，使用黑色文字；不随扇形颜色改变。
- 下部显示两个较大圆角长方形按钮：“完成”和“重抽”。
- “完成”为绿色，“重抽”为红色。
- “重抽”按钮下方显示提示：“重抽只有一次机会，选择后选择权将不在您的手中”。

点击“完成”：
- 结束当前玩家这一轮。
- 返回号码转盘。
- 号码转盘保留此前停下的角度，配色和编号不变。
- 题目转盘也保留刚刚停下的角度。
- 新一轮恢复普通“开始”按钮。

点击“重抽”：
- 立即消耗本轮唯一一次重抽机会，关闭结果框。
- 继续使用本轮同一档位、同一类别、同一个题目转盘。
- 从该题目转盘当前角度开始强制旋转，不归零。
- 本次重抽不得再次抽中刚才那一条题目；其他99条均可参与。
- 从启动到完全停稳的总时长随机控制在3—5秒，已经包含加速与减速。
- 仍然使用最高720°/秒、约1秒加减速和相同的平滑曲线。
- 两侧开始／暂停按钮完全隐藏，不只是禁用，也不显示替代按钮。
- 中央保留转盘；左上角退出“×”依旧存在。
- 用户不能控制重抽的停止时间，也不能再换真心话／大冒险类别。
- 停稳后保留轮盘画面2秒。
- 然后显示覆盖整个可用视口的最终结果层。
- 最终题目为醒目的红色大字；下方只有一个居中的大型绿色圆角长方形“已完成”按钮。
- 最终结果不显示重抽按钮。
- 点击“已完成”后返回号码转盘，各轮盘保留自己的角度。
- 重抽机会属于“每位玩家每一轮”，不是每场游戏总共只有一次。下一轮恢复一次机会。

为了同时满足“3—5秒”“不能重抽同一题”“指针与结果一致”，重抽应在开始时一次性规划：
- 在其余99个题目ID中等概率选择一个目标，并在目标扇形内部选取目标角度。
- 避开扇形分界线，规划所需的完整圈数和持续时间。
- 对上述镜像速度曲线，完整加速和减速情况下，总转角为720×(T−d)度。
- 若目标相对当前角度的顺时针差值为δ，则可以从满足3≤T≤5的整数圈数k中随机选取，令T=d+(δ+360k)/720。
- 指针按该计划真实旋转到目标位置。最终仍从停止角度反查题目ID并验证一致，不能显示与实际落点不同的内容，也不能结束时跳针。

**9．会话与角度保存**

明确区分：
- “一场游戏”：从首页点击开始建立，直到点击“×”并确认退出。
- “一轮游戏”：抽中一名玩家，选择类别并完成题目或重抽后的题目。
- “一次转动”：某个轮盘的一次启动、旋转、减速和停止。

本场至少保存：
- 档位、人数、本场配色与题目顺序。
- 号码转盘角度、真心话转盘角度、大冒险转盘角度，三者独立保存。
- 当前流程、当前玩家、题目ID、本轮是否已经重抽。
- 正在转动时的曲线参数、已运行时间、必要的重抽目标及剩余等待时间。

三个轮盘首次出现时各自指向12点钟；以后分别从各自上次停下的位置继续。不能将号码盘角度复制给题目盘，也不能因为组件重新挂载而归零。

本版采用的其他默认行为：
- 同场不同轮允许抽中相同玩家，也允许抽中以前出现过的题目。
- 只保证“一次重抽”不会再拿到它刚刚替换的那一道题。
- 完成题目后不移除扇形，不改变100等分结构。
- 不增加计分、排行榜、账号、联网房间或玩家惩罚等未要求的功能。

退出确认与中断处理：
- 打开退出框时，冻结当前转动、2秒等待计时和页面转换；取消后从冻结点继续。
- 确认退出时，清理动画、计时器和本场数据，不能出现旧结果框延迟弹回首页。
- 应用失去可见性时暂停游戏时间，回来后继续，避免后台时间导致突然跳结果。
- 刷新不应重抽、重置角度或恢复重抽机会。使用本地会话保存当前进度，刷新恢复后继续原进度。
- 首版以同一标签页的会话为范围，不要求跨设备或跨标签页同步。

**10．角度判定与防争议**

所有轮盘共用一个判定方法：
- 正上方为0°，顺时针为正。
- 有N格时，单格角度w=360°/N。
- 第1格中心线为0°；格子中心依次为0°、w、2w……。
- 令θ为指针相对正上方的顺时针角度：
  - 先计算a=((θ+w/2)%360+360)%360；
  - 再计算索引i=floor(a/w)，结果限制在0至N−1。
- 号码结果为i+1；题目结果为该固定索引绑定的题目ID。
- 扇形统一使用起始边界包含、结束边界不包含的规则；恰在分界线上时归属于顺时针方向接下来的扇形。
- 绘制分界线、指针尖端、计算结果必须使用同一坐标系，不能因为CSS旋转方向或起始偏移不同而错一格。
- 最终角度只结算一次，并保存该角度和结果；弹窗不能重新随机抽一遍。
- 指针内部可以保存累计转角，以保证动画连续；显示和结果判定时再规范到0—360°。

**11．交付与验收**

请交付可运行的完整网页、分离的题库数据，以及简短的启动说明。

至少逐项检查以下实际行为：
- 三档各有真心话100条、大冒险100条，总计600条，且每组没有重复或占位题。
- 人数提交、回车、异常输入处理正常。
- 号码等分、12点钟在1号内部、顺时针编号正确。
- 左右按钮文字分别旋转+90°、−90°，两侧操作始终同步。
- 圆盘固定、指针顺时针转动，最高速度为720°/秒。
- 手动暂停平滑，提前暂停不跳速；无人操作时10秒内停稳。
- 自动停止不会因固定整圈问题总回到起始方向。
- 号码停稳等待2秒，首次题目停稳直接显示结果，重抽停稳等待2秒。
- 弹窗尺寸、直角外框、背景压暗、按钮造型与颜色符合约定。
- 100格完整文字按径向排列，没有跨扇形溢出；弹窗内容与尖端落点一致。
- 重抽总时长3—5秒、隐藏两侧按钮、不能再次抽到原题、不能进行第二次重抽。
- 最终结果为全屏红色题目文字，只有“已完成”这一项结果操作。
- 号码盘与两类题目盘各自保留角度，下一轮不归零。
- 从任意游戏阶段打开退出框，取消可以恢复；确认才清空本场。
- 重复点击、旋转屏幕、切换全屏和刷新不会产生双动画、旧弹窗或额外重抽机会。
- 在实际平板比例下检查布局和触控可用性，并至少演练一次“普通完成”和一次“重抽后完成”的完整路径。
